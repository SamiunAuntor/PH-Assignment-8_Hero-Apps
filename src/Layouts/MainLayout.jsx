import React from 'react';
import NavBar from '../Components/NavBar';

const MainLayout = () => {
    return (
        <div>
            <NavBar></NavBar>
        </div>
    );
};

export default MainLayout;